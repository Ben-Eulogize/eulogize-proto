import styled from 'styled-components'
import { default as AntRadio } from 'antd/lib/radio'
import { STYLE } from '@eulogise/client-core'

// @ts-ignore
export const Radio: any = styled(AntRadio)`
  font-size: ${STYLE.TEXT_FONT_SIZE_SMALL};
`
